# Responsive To-Do List with Local Storage
My first project-based learning project. This is a simple To-Do List application that uses `localStorage` to save tasks even after refreshing the browser.

## Features
* Add and Delete tasks
* Persistent storage using JSON
* Responsive design
* Adding a Day Schedule
* Adding a Time Schedule